# Versions are pinned to the minor releases the system was developed, tested and
# measured with (docs/EXPERIMENTS.md). ADK's internals (callback signatures,
# LiteLlm kwargs, lifespan) and FastAPI/Starlette routing changed between
# releases; CI on an unpinned install ran ADK 1.39 / FastAPI 0.141 and broke.
# [db] pulls in SQLAlchemy, required by DatabaseSessionService (a bare install could not start the app)
google-adk[db]>=1.22.0,<1.23
google-genai>=0.3.0
litellm>=1.80.0,<1.81
python-dotenv>=1.0.0
pydantic>=2.0.0

# API Server dependencies
uvicorn[standard]>=0.40.0,<0.41
aiosqlite>=0.19.0
fastapi>=0.123.0,<0.124

# Cloud Database (Neon PostgreSQL)
asyncpg>=0.30.0
psycopg2-binary>=2.9.0

# Web Search tools (SerpAPI + content extraction)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Image Analysis dependencies
httpx>=0.26.0

# Scheduled report generation
apscheduler>=3.10.0

# Firebase Cloud Messaging (push notifications)
firebase-admin>=6.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.3.0
